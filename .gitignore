# ── Decks (Day1/, templates/) ──
# LibreOffice / render artefacts
Day1/**/*.pdf
templates/**/*.pdf
render/
~$*.pptx
.DS_Store

# ── App: Python ──
__pycache__/
*.pyc
.venv/
venv/
.env

# Trained model artifacts are rebuilt from data by scripts/train_models.py
# (they are version-locked to the installed sklearn/xgboost, so never commit them)
backend/models/*
!backend/models/.gitkeep

# RAG embedding cache (built lazily at runtime when GEMINI_API_KEY is set) and runtime state
backend/data/runtime/embeddings*
backend/data/runtime/ragembed-*
backend/data/runtime/queue.json
backend/data/runtime/audit.json
backend/data/runtime/agent_eval_results.json
backend/data/runtime/model_state.json
backend/data/runtime/ram_sessions.json*

# Full FHIR export is regenerated on demand (only the sample is committed)
backend/data/fhir_full/

# ── App: Node ──
node_modules/
frontend/dist/

# Misc
*.log

# per-team OAuth client secrets written by bootcamp/viya/create_team_clients.py
bootcamp/viya/clients*.csv
